# Required — JWT signing secret for NextAuth. Generate with:
#   openssl rand -base64 32
NEXTAUTH_SECRET=

# Base URL of the app. Auto-detected in dev; required in production.
NEXTAUTH_URL=http://localhost:3000
